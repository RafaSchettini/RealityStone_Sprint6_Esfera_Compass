default: --format pretty -p prd -p chrome
default_hml: --format pretty -p hml -p chrome


# Reporters
# html: --format html --out=reports/report.html

# Environments
prd: ENVIRONMENT="prd"
hml: ENVIRONMENT="hml"

# Browsers
chrome: BROWSER="chrome"

# Visual Mode
headless: HEADLESS="true"