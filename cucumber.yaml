default: -p prd -p chrome -p html


# Reporters
html: --format html --out=reports/report.html

# Environments
prd: ENVIRONMENT="prd"

# Browsers
chrome: BROWSER="chrome"

# Visual Mode
headless: HEADLESS="true"